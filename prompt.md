# Starter Prompt

Use `$supply-chain-guard` in this repo. Before any install, update, scaffold, package-manager command, CI action/workflow change, IDE/MCP/agent-tool change, or dependency-provided tool execution, inspect the manifests and lockfiles, load the relevant reference playbook, preserve locked installs, verify package identity and age, disable lifecycle scripts by default, verify provenance as identity evidence only, and stop for approval if a dependency, installer, workflow, or tool cannot be verified.
